---
title: Console
description: "Navigate caracal-console: launch, guided setup, key bindings, views, and audit explanation."
pageType: reference
---

`caracal-console` is the Console for setting up and operating protected agent access without leaving the shell. It fetches from the management API and renders into a full-screen Console buffer with keyboard navigation.

---

## Launch

```bash
caracal-console
```

**Requirements:**

- A TTY Console (`stdin` must be a TTY).
- An admin token discoverable from `CARACAL_ADMIN_TOKEN`, `CARACAL_ADMIN_TOKEN_FILE`, or the local secret files generated by `caracal up`.
- `CARACAL_API_URL` set in production (localhost default in development).
- For agent and delegation views: `CARACAL_COORDINATOR_URL` and a Coordinator token discoverable from `CARACAL_COORDINATOR_TOKEN`, `CARACAL_COORDINATOR_TOKEN_FILE`, or the local secret files generated by `caracal up`.

For default localhost services, generated local secret files are preferred over exported token values so a local `caracal purge` and `caracal up` cycle does not leave Console using stale shell tokens. For non-default services, explicit env vars and `*_FILE` variables keep their normal override behavior.

On startup, the Console enters full-screen alt buffer mode and hides the cursor. Press `q` or `ctrl-c` at any time to exit and restore the TTY.

---

## Screen layout

```
┌──────────────────────────────────────────────────────────────────┐
│ Caracal Console                                   http://localhost:3000│  banner
│ > menu › zones › zone_abc123                                      │  breadcrumb
├──────────────────────────────────────────────────────────────────┤
│                                                                  │
│  (view content)                                                  │
│                                                                  │
├──────────────────────────────────────────────────────────────────┤
│ (status message)                                                 │  status line
│ ↑/↓:move  enter:select  z:set-zone  h:back  q:quit             │  key hints
└──────────────────────────────────────────────────────────────────┘
```

The Console is redrawn every 30 ms when content changes. Resize events (SIGWINCH) trigger an immediate redraw. The last column in every list view expands to fill the remaining Console width.

---

## Main menu

The main menu is the root view. Navigate with arrow keys or number shortcuts:

| Key | View | Zone required |
| --- | --- | --- |
| `s` | Guided setup | No |
| `1` | Zones | No |
| `2` | Applications | Yes |
| `3` | Protected resources | Yes |
| `4` | Providers | Yes |
| `5` | Access policies | Yes |
| `6` | Policy sets | Yes |
| `7` | Access grants | Yes |
| `8` | Authority sessions | Yes |
| `9` | Control API | Yes |
| `a` | Audit | Yes |
| `e` | Explain audit decision | Yes |
| `r` | Agent runs | Yes + Coordinator token |
| `g` | Delegation graph | Yes + Coordinator token |
| `d` | Diagnostics | No |
| `c` | Credential tools | No |

For views that require a zone, press `z` to open the zone picker before navigating. New users should start with guided setup, which asks only for values Caracal cannot already infer: zone when none is selected, the agent app, the protected resource, an optional external upstream URL, the credential provider needed for that upstream, and Caracal scopes. Provider fields adapt to OAuth/OIDC, API-key, and workload identity choices; direct resources can leave the upstream and provider prompts blank. First request examples, policy skipping, local file writes, manual identifiers, and profile paths stay in Advanced settings so the first setup path remains short.

---

## Navigation: keys that work everywhere

| Key | Action |
| --- | --- |
| `q` | Quit |
| `ctrl-c` | Quit |
| `h`, `esc`, `←` | Go back to the parent view |
| `↑`, `j` | Move cursor up one row |
| `↓`, `k` | Move cursor down one row |
| `pgup` | Move up 10 rows |
| `pgdn` | Move down 10 rows |
| `home`, `g` | Jump to the first row |
| `end`, `G` | Jump to the last row |
| `enter` | Open selected row |
| `r` | Reload data from the API |
| `z` | Open zone picker |
| `?` | Open focused contextual info |

Forms show a focusable **Advanced options** row when a section has uncommon settings. Move to that row and press `→` or `enter` to open a separate advanced-options page for that form. Advanced fields include manual identifiers, raw JSON, shadow versions, overwrite flags, and other settings that are useful for enterprise or non-standard cases but are safe to leave blank on the common path.

---

## Zone picker

Press `z` from the main menu or any list view to change the active zone. All zone-scoped views use the selected zone.

The zone picker lists all zones with columns: `slug`, `id`, `name`. The currently selected zone is highlighted.

Navigate with the standard list keys. Press `enter` to confirm the selection and return to the previous view. Press `h`, `esc`, or `←` to cancel without changing the zone.

---

## List views

Each entity type opens its own list view with fixed-width columns and an expanding final column. The status line shows loading state, errors, and the row count.

**Loading states:**

- `loading…`: initial fetch in progress.
- `(no rows)`: fetch succeeded with an empty result set.
- Error message in red: fetch failed. Common errors:
  - `401 Unauthorized`: check `CARACAL_ADMIN_TOKEN`.
  - `403 Forbidden`: token lacks required scope.
  - `404 Not Found`: resource may have been deleted since the previous load.
  - Connection error: check `caracal status` to confirm the stack is running.

**Columns per view:**

| View | Columns |
| --- | --- |
| Zones | `name` (24), `slug` (18), `login_flow` (12), `dcr` (5), `pkce` (5) |
| Applications | `name` (24), `method` (8), `cred` (12), `traits` (24) |
| Protected resources | `name` (24), `identifier` (30), `upstream` (32), `Caracal scopes` |
| Providers | `name` (24), `identifier` (24), `kind` (10), `owner` (10) |
| Policies | `name` (28), `owner` (10), `description` (32) |
| Policy sets | `name` (24), `active version` (16), `description` |
| Grants | `app` (28), `subject` (36), `resource` (28), `status` (10), `Caracal scopes` |
| Sessions | `subject` (36), `type` (10), `status` (10), `expires_at` (24) |
| Agents | `application` (28), `parent` (36), `status` (10), `depth` (6), `spawned_at` (24) |

Text that exceeds a column's width is truncated with `…`. Internal IDs are hidden by default; use the reveal/copy shortcuts only when another system needs the exact ID.

---

## Detail views

Press `enter` on any list row to open the detail view for that object. The detail view renders structured fields and masks secret-shaped values until you explicitly reveal them.

**Navigation in detail view:**

| Key | Action |
| --- | --- |
| `↑`, `j` | Scroll up one line |
| `↓`, `k` | Scroll down one line |
| `pgup` | Scroll up 10 lines |
| `pgdn` | Scroll down 10 lines |
| `home`, `g` | Jump to the top |
| `end`, `G` | Jump to the bottom |
| `r` | Reload from the API |
| `h`, `esc`, `←` | Back to the list view |

---

## Audit tail (live streaming)

The Audit view (`9` from the main menu) polls the audit API every 2 seconds and displays events as they arrive. Up to 500 events are kept in memory.

**Columns:** `occurred_at` (22), `event_type` (28), `decision` (8), `status` (12), `request_id` (24)

**Decision coloring:**

| Decision | Color |
| --- | --- |
| `allow` | Green |
| `deny` | Red |
| `partial` | Orange |

**Audit-specific keys:**

| Key | Action |
| --- | --- |
| `p` | Toggle pause / resume streaming |
| `d` | Cycle decision filter: `all` → `allow` → `deny` → `partial` → `all` |
| `enter` | Open full audit detail for the selected event |
| `r` | Force immediate reload |

The current filter is shown in the status line (e.g., `filter:deny`). When paused, `p:resume` is shown. When streaming, `p:pause` is shown.

**Audit detail:**

Press `enter` on an audit event to open its full detail view, showing the complete `AuditDetail` record including `determining_policies_json` and `diagnostics_json`.

---

## Error display

API errors are shown in the status line in red. The interface does not exit on API errors; press `r` to retry, or press `h` to go back and try a different view.

| Error | Likely cause |
| --- | --- |
| `unauthorized: check CARACAL_ADMIN_TOKEN` | Token missing, wrong, or expired |
| `forbidden: token lacks required scope` | Token does not have admin scope |
| `not found: resource may have been deleted` | Entity was removed between load and detail fetch |
| `is the stack running? try 'caracal up'` | API not reachable |
| `coordinator URL not set` | `CARACAL_COORDINATOR_URL` is missing |
| `coordinator token not found` | Run `caracal up` or set `CARACAL_COORDINATOR_TOKEN` / `CARACAL_COORDINATOR_TOKEN_FILE` for a non-default Coordinator |
