{
  "name": "@caracal-examples/echo-upstream",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "Zero-dependency echo upstream that proves Gateway-brokered requests reach a protected target.",
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "start": "node server.mjs",
    "test": "node --test"
  }
}
