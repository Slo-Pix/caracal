# Copyright (C) 2026 Garudex Labs.  All Rights Reserved.
# Caracal, a product of Garudex Labs
#
# Python package metadata for the ASGI connector.

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "caracalai-asgi"
version = "0.1.5rc3"
description = "ASGI middleware for Caracal mandate verification (FastAPI, Starlette, Quart)"
license = "Apache-2.0"
license-files = ["LICENSE"]
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
  "caracalai-transport-mcp==0.1.5rc3",
  "caracalai-revocation==0.1.5rc3",
]

[tool.hatch.build.targets.wheel]
packages = ["caracalai_asgi"]
