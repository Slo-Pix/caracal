# cloudflare

## Scope
- Covers the Cloudflare Workers runtime adaptor; per-language implementations live in subdirectories.

## Required
- Each language implementation must live in its own subdirectory (`ts/`).

## Forbidden
- Must not contain implementation code at this level.
